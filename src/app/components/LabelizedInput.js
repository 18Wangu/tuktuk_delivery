export const LabelizedInput = ({ label, value, onChange, disabled }) => (
    <form>
      {/*<label>
        {label}
        {": "}
      </label>
      <input disabled={disabled} value={value} onChange={onChange} />
      */}
    </form>
  );
  